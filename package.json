{
  "name": "md-reader",
  "version": "1.0.0",
  "private": true,
  "description": "零依赖单文件 Markdown 阅读器：双击即用、离线可用、支持拖入整个文件夹",
  "scripts": {
    "build:hljs": "node scripts/build-hljs.js",
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch"
  }
}
